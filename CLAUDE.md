# AmsalGame_proto — 프로젝트 컨텍스트

## 프로젝트 개요
탑뷰 암살 멀티플레이어 게임
- Unity 6.3 LTS, URP, 3D Core
- IDE: VSCode + Claude Code (에이전트)
- 언어: C#
- 버전관리: Git / GitHub
- 출시 목표: Steam (Windows)

## 핵심 철학
"보이지 않는 것이 더 무섭다"
- 사운드가 핵심 게임플레이 메커닉
- 항상 움직여야 하는 긴장감 유지
- 암살 우선, 불가피할 때 CQC
- 개인 행동과 정보전 중심

---

## 구현 현황 (Phase 1)

### ✅ 완료
| 시스템 | 스크립트 | 비고 |
|--------|----------|------|
| 플레이어 이동 | `PlayerController.cs` | WASD + Shift(걷기), CharacterController |
| 요원 데이터 | `AgentData.cs` | ScriptableObject, Offensive/Defensive/Neutral |
| 탑뷰 카메라 | `TopViewCamera.cs` | 스무스 팔로우, 75° 기울기 |
| 사운드 이벤트 | `SoundEventSystem.cs` | 싱글톤, Action\<SoundEvent\> 브로드캐스트 |
| 발소리 발생 | `SoundEmitter.cs` | 0.3초 간격, 이동 중만 emit |
| AI FSM | `AIController.cs` | NavMesh 기반, FOV + 청각 탐지 + Combat 상태 |
| 암살 | `AssassinationSystem.cs` | LMB(컨텍스트 감지), 1.5m, 후방 120도 |
| 체력 | `Health.cs` | maxHp=3, OnDeath 이벤트 |
| CQC 근접전투 | `CombatController.cs`, `PlayerCombatInput.cs` | LMB 공격/암살, RMB 막기/패링, F 그랩 |
| 존버 방지 | `ExposureSystem.cs` | 정지 타이머, 경고/노출 이벤트, 방어형 보너스 |
| HUD (일부) | `AssassinationIndicatorUI.cs`, `ExposureGaugeUI.cs`, `GameResultUI.cs` | 암살 인디케이터, 노출 게이지/경고 오버레이, 승패 배너 |
| FOV (플레이어) | Editor 자동화로 배선 (스크립트 없음) | Player 자식 SpotLight + 실시간 그림자, 태양광 감광 |
| 원거리 무기 | `RangedWeaponController.cs`, `ThrownKnife.cs`, `WeaponPickup.cs` | 칼 던지기(Q/R) + 권총 파밍(MMB 발사) |
| 기본 맵 (A/B 사이트) | Editor 자동화(`SetupSites`) | 마커 + 권총 파밍 지점, ProBuilder 블록아웃 재사용 |
| 군집 페널티 | `TeamMember.cs`, `ClusterPenaltySystem.cs` | 제네릭 구현 — 팀원이 Player 1명뿐이라 실전 검증은 멀티플레이 phase에서 |
| 승리/패배 조건 | `GameStateManager.cs` | 전멸=승리, Player 사망=패배 |

씬 배선은 `Assets/Scripts/Editor/Phase1SceneSetup.cs` (Tools → AmsalGame → Phase 1 씬 자동 구성)로 자동화됨.

### 추가 작업 (Phase 1 이후 보강)
- 맵 확장: `Tools → AmsalGame → 맵 확장 (방 2개 + 복도 추가)` — 기존 방(57.8m×57.8m 실측)에서 벽 2개를 문 있는 벽으로 교체하고 복도(폭 3m)로 연결된 Room B/Room C(7×7m)를 절차적으로 생성. 방 크기는 Player 위치에서 Raycast로 실측(메시 bounds는 신뢰 불가 — 아래 기록 참고). `Phase1SceneSetup.cs`의 `ExpandMap()`.
- 적 앞/뒤 색상 마커: Enemy 캡슐 자식으로 빨강(정면)/초록(후방, 암살 가능 구간) 구슬 부착 — `SetupFacingMarkers()`.

### 🔲 미착수 (Phase 1 잔여)
- 미니맵 (사운드 핑, 노출 실루엣, 레이더 노출 — 관련 시스템들은 이벤트만 발행 중이고 소비하는 미니맵 UI 자체가 없음)
- 총알 별도 파밍 (현재는 권총 파밍 시 탄약도 함께 획득하는 것으로 단순화함)
- FOV 라이팅 수치 튜닝 (SpotLight 밝기/각도, 태양광 감광 정도는 1차 추정치)

### ⚠️ 알아두면 좋은 사실
- `Assets/Scenes/SampleScene.unity`가 세션 도중 텍스트(YAML)에서 Unity 바이너리 직렬화로 바뀜 — Claude Code가 더 이상 grep으로 씬을 못 읽음. Editor 자동화 스크립트가 Unity API(GameObject.Find, Renderer.bounds, Physics.Raycast)로 값을 읽는 방식이라 기능엔 문제 없지만, 앞으로 씬 관련 문의 시 텍스트 파싱이 아니라 Editor 스크립트로 조회해야 함.
- ProBuilder로 만든 벽/바닥의 `Renderer.bounds`는 신뢰 금지 — 한 번 실제 크기(900+ 단위)로 완전히 잘못된 값을 반환한 적 있음. 크기가 중요한 배치는 `Physics.Raycast`로 실측할 것 (`ExpandMap`의 `MeasureHalfExtent` 참고).

---

## 입력 시스템
Unity Input System (새 방식) 사용 — `InputSystem_Actions` 자동생성 클래스

| 액션 | 실제 키 | 게임 내 역할 |
|------|---------|-------------|
| Move | WASD | 이동 방향 |
| Sprint | Left Shift | **걷기 모드** (속도 감소, 발소리 최소화) |
| Attack | LMB | 컨텍스트 감지형 — 암살 가능하면 암살, 아니면 CQC 공격 |
| (직접 폴링) | RMB | 막기(홀드) / 누른 직후 짧은 구간 패링 |
| (직접 폴링) | F | CQC 그랩 (막기 중인 상대에게만 유효) |
| (직접 폴링) | Q | 칼 던지기 (마우스 방향) |
| (직접 폴링) | R | 던진 칼 회수 (1.5m 이내) |
| (직접 폴링) | MMB | 권총 발사 (파밍 후 1회, 소모성) |

> Sprint 액션을 "걷기"로 역용 — Shift를 누르면 느려지는 설계
> RMB/F/Q/R/MMB는 InputSystem_Actions 액션으로 등록되어 있지 않아 `Mouse.current`/`Keyboard.current`로 직접 폴링 (`PlayerCombatInput.cs`, `RangedWeaponController.cs`). 추후 액션 에셋 정리 시 정식 액션으로 승격 검토 — 특히 콘솔 대응 시 게임패드 바인딩이 없는 입력들이라 우선순위 높음.

---

## 이동 시스템
| 상태 | 속도 | 발소리 반경 |
|------|------|-------------|
| 이동 | 요원별 고유 속도 | 중간 |
| 걷기 (Shift) | 기본의 50% (AgentData 기본값) | 최소 |
| 웅크리기 | 추후 검토 | — |

**AgentData 기본값 (ScriptableObject)**
| 필드 | 기본값 | 설명 |
|------|--------|------|
| moveSpeed | 3.5f | 기본 이동 속도 (m/s) |
| walkSpeedMultiplier | 0.5f | 걷기 속도 배율 |
| moveSoundRadius | 8f | 이동 발소리 반경 (m) |
| walkSoundRadius | 3f | 걷기 발소리 반경 (m) |

PlayerController에 AgentData가 없을 때 폴백: 속도 3.5f, 걷기 배율 0.6f

---

## 사운드 메커닉 (핵심 게임플레이)
| 행동 | 소리 범위 |
|------|-----------|
| 이동 | 중간 반경 |
| 걷기 | 최소 반경 |
| 총소리 | 맵 전체 + 위치 노출 |
| 칼 던지기 | 무음 (빗나가면 착지음) |
| 근접 암살 | 무음 |
| 소리 트랩 발동 | 트랩 반경 내 전파 |

사운드는 메인 화면이 아닌 좌상단 미니맵에 핑으로 표시
- 핑 크기 = 소리 거리/크기 비례
- 잠깐 떴다 사라지는 방식

---

## 전투 시스템

### 암살 (주력) ✅ 구현됨
- 적이 나를 인식하지 못한 상태에서 후방 접근 시 발동
- 즉사, 무음 처리
- **입력: LMB** (컨텍스트 감지 — `AssassinationSystem.CanAssassinate`가 true면 암살, 아니면 CQC 공격으로 분기. `PlayerCombatInput.cs`)
- 암살 가능 거리: 1.5f
- 암살 가능 각도: 적 후방 120도 이내
- AI가 Chase/Combat 상태일 때 암살 불가 (인식 여부 판정)
- 암살 가능 시 적 머리 위에 월드스페이스 인디케이터 표시 (`AssassinationIndicatorUI.cs`)

### CQC — 근접 전투 ✅ 구현됨 (`CombatController.cs`)
발동 조건: AI가 Chase 상태에서 근접 거리(기본 2m)로 진입 시 자동으로 Combat 상태 전환 → 서로 교전

| 입력 | 행동 | 설명 |
|------|------|------|
| LMB | 공격 | 기본 타격 (2 dmg, 막기 중 상대는 1 dmg) | 
| RMB | 막기 | 피해 감소, 막기 중엔 본인 공격/그랩 불가 |
| RMB 타이밍 | 패링 | 막기 시작 직후 0.3초 이내 피격 시 상대 경직 |
| F키 | 잡기 | 막기 중인 상대에게 유효, 즉시 처치 |

CQC 흐름:
- 패링 성공 → 상대 경직(`IsStaggered`) → 이후 LMB로 암살 피니시 가능 (즉사)
- 기본 공격 히트 → 체력 감소
- 잡기 성공 → 상대 무력화 → 즉시 처치

AI 측 CQC 행동은 `AIController.DecideCombatAction()`의 단순 확률 기반 플레이스홀더(60% 공격 / 40% 막기, 0.8~1.6초 간격) — 실제 전투 AI 튜닝은 밸런싱 예정.

### 원거리 (극히 제한적) ✅ 구현됨 (`RangedWeaponController.cs`, `ThrownKnife.cs`, `WeaponPickup.cs`)

**칼 던지기 (기본 보유)**
- Q: 마우스 방향 발사, R: 1.5m 이내 접근 시 회수
- 무음, 빗나가면 착지음(반경 6m) 발생 → 주변 AI/플레이어 반응
- 6초 내 회수 못하면 자동 소멸 (안전장치, 스펙 외 추가 구현)

**권총 (파밍 무기)**
- 시작 시 미보유, A/B 사이트(`SetupSites`로 배치)에서 파밍해야 사용 가능
- 스폰: 맵 내 A/B 2개 사이트 각 1자루, 총 2자루 제한
- ~~총알 별도 스폰~~ → 파밍 시 탄약도 함께 획득하는 것으로 단순화 (1발 소모성 무기라 별도 탄약 엔티티의 실익이 적어 스코프 축소)
- 사용(MMB): 1발 발사 후 소모, 벽/적 판정 레이캐스트(사거리 30m)
- 성능: 명중 시 즉사
- 리스크: 발사 시 맵 전체 사운드 → 위치 노출 (반경 사실상 무한대로 처리)
- 라운드 리셋 시 재스폰: 미구현 (라운드 시스템 자체가 없음 — Phase 2 이후 과제)
- 게임플레이 의도: A/B 사이트 거점 경쟁 + 위험 동선 트레이드오프

---

## 요원 시스템
모든 요원 동일 외형, 유틸리티 스킬로 차별화
AgentType enum: `Offensive` / `Defensive` / `Neutral`

### 🛡️ 방어형 (Defensive)
이동속도: 느린 편
맵 통제 + 정보 수집 + 함정 설치 특화
존버 패널티 정지 허용 시간: 기본 +2~3초 (함정 설치 고려)

| 스킬 | 설명 |
|------|------|
| 소리 트랩 | 적이 밟으면 소리 이벤트 발생, 위치 노출 |
| 부비트랩 | 적이 밟으면 피해 + 경직, CQC 기회 창출 |
| 침묵 구역 | 반경 내 발소리 차단, 아군 은신 지원 |
| 페이크 사운드 | 다른 위치에서 발소리 발생, 적 유인 |

### ⚔️ 공격형 (Offensive)
이동속도: 빠른 편
기동력 + 암살 기회 창출 특화
존버 패널티 정지 허용 시간: 기본과 동일

| 스킬 | 설명 |
|------|------|
| 대시 | 짧은 거리 순간이동, 발소리 최소화 |
| 집중 | 이동속도 대폭 증가, 발소리 소폭만 증가 |
| 무음 질주 | 빠르게 이동하지만 발소리 반경 대폭 감소 |
| 마킹 | 벽 너머 적 위치를 짧은 시간 파악 |

### 팀 조합 시너지
- 방어형이 함정으로 적 위치 파악 → 공격형이 빠르게 암살
- 공격형이 교란 → 방어형이 도주로에 트랩 설치
- 팀원은 최대 2명까지만 같이 행동 권장 (군집 페널티 참고)

---

## 군집 페널티 시스템 ✅ 구현됨 (`TeamMember.cs`, `ClusterPenaltySystem.cs`)
아군끼리 뭉치는 플레이 억제 → 개인 행동 유도

> 현재 싱글플레이 프로토타입에는 `TeamMember`를 가진 개체가 Player 1명뿐이라 3인 집결 조건이 실전에서 트리거되지 않음. 감지 로직(발소리 배율, CQC 피해 배율, 암살 무효)은 이미 `SoundEmitter`/`CombatController`/`AssassinationSystem`에 연결돼 있어 Phase 3에서 팀원이 추가되면 바로 검증 가능. 레이더 노출은 미니맵이 없어 미구현.

### 2명까지 — 정상
- 암살 판정 정상
- 발소리 정상
- CQC 피해 정상

### 3명 이상 동일 반경 내 집결 시 — 페널티 발동

| 페널티 | 효과 |
|--------|------|
| 레이더 노출 | 적 미니맵에 위치 실시간 노출 |
| 발소리 누적 | 인원수 × 1.5배 반경 증가 |
| CQC 피해 증가 | 근접 전투 시 받는 피해 증가 (밸런싱 예정) |
| 암살 무효 | 즉사 판정 → CQC 강제 전환 |

### 설계 의도
- 뭉치면 안전해 보이지만 실제론 더 위험한 구조
- 암살 게임 컨셉 유지 → 개인 행동, 정보전 강제
- 팀원 2명까지는 협력 가능 (완전 고립 방지)

### 밸런싱 예정
- 군집 감지 반경 수치
- CQC 피해 증가율
- 레이더 노출 주기/지속 시간

---

## 존버 방지 시스템 (노출 페널티) ✅ 구현됨 (`ExposureSystem.cs`)
플레이어가 일정 시간 정지 시 맵에 위치 노출. 노출은 한 번 시작되면 이동해도 정해진 시간만큼 유지(이동으로 페널티 즉시 회피 방지), 경고 단계는 이동 즉시 초기화.

| 항목 | 현재 기본값 (밸런싱 예정) |
|------|----|
| 정지 허용 시간 | 6초 |
| 경고 카운트다운 | 2.5초 |
| 노출 지속 시간 | 1~3초 랜덤 |
| 초기화 조건 | 이동 시작 즉시 (경고 단계만 — 노출 발동 후엔 유지) |
| 방어형 요원 | 정지 허용 시간 +2.5초 |

플레이어 본인: 우상단 게이지(`ExposureGaugeUI.cs`, 경고 진행률) + 노출 시 화면 전체 빨간 오버레이로 근사 구현 (스펙상 "가장자리" 비네트는 추후 비주얼 폴리싱 필요)
적팀: `OnExposureStart`/`OnExposureEnd` 이벤트만 발행 — 미니맵 핑/실루엣은 미니맵 시스템 자체가 없어 미착수

사운드 시스템과의 시너지:
- 가만히 있으면 위치 노출
- 함부로 뛰면 발소리로 위치 노출
- 걷기의 전략적 가치 극대화

---

## AI 시스템
상태머신 (FSM) 기반, NavMeshAgent 사용

| 상태 | 설명 |
|------|------|
| Idle | 제자리 대기 (1~3초 랜덤) |
| Patrol | 스폰 기준 반경 10m 내 랜덤 웨이포인트 순찰 |
| Suspicious | 소리 감지 위치로 이동 후 5초 탐색 |
| Chase | 시야 내 플레이어 발견, NavMesh 추격 |
| Alert | 동료 사망 감지, 4초 경계 후 복귀 |
| Combat | Chase 중 근접 거리 진입 시 전환, CQC 교전 (`CombatController` 연동) |

**AIController 기본값**
| 항목 | 값 |
|------|----|
| viewDistance | 8f |
| viewAngle | 90° |
| hearingRadius | 15f |
| patrolRadius | 10f |
| patrolSpeed | 2f |
| chaseSpeed | 4.5f |
| suspiciousDuration | 5f |
| alertDuration | 4f |
| combatEngageRange | 2f |
| combatDisengageRange | 3.5f |

공개 메서드: `TriggerAlert()` — 동료 사망 시 외부에서 호출 (아직 연결 미완료)

---

## HUD 구성
| 위치 | 요소 |
|------|------|
| 좌상단 | 미니맵 (사운드 핑, 레이더 노출 표시) |
| 우상단 | 존버 방지 게이지 + 경고 |
| 좌하단 | 요원 정보 (타입 / HP 숫자+바 / 스킬) |
| 우하단 | 무기 (아이콘 + 상태 표시) |

시야(FOV): ✅ 구현됨 (커스텀 셰이더 없이 라이팅으로 근사)
- Player 자식에 SpotLight(각도 100°, 사거리 14m, 실시간 그림자) 부착 → 벽에 막히면 자연스럽게 차폐
- 태양광(Directional Light) 감광(2 → 0.25)으로 시야 밖은 어둡게
- 정석적인 부채꼴 시야 셰이더(스텐실/포스트프로세스)가 아니라 엔진 기본 라이팅 기능만으로 구현한 근사치 — Claude Code가 Unity Editor에서 직접 시각 확인을 할 수 없어 셰이더 작성 대신 검증 리스크가 낮은 방식을 택함. 밝기/각도 수치는 1차 추정치라 실제로 봤을 때 튜닝 필요할 가능성 높음

---

## 개발 로드맵

### Phase 1 — 싱글 프로토타입 (현재 목표)
개발 순서:
1. ✅ 플레이어 이동 (걷기 + 요원별 속도)
2. ✅ 요원 시스템 기본 틀
3. ✅ 탑뷰 카메라
4. ✅ 기본 맵 (ProBuilder 블록아웃 기존 완료 + A/B 사이트는 이번에 Editor 자동화로 배치)
5. ✅ 사운드 이벤트 시스템
6. ✅ AI 기본 (Patrol → Suspicious → Chase)
7. ✅ 암살 시스템
8. ✅ CQC 시스템 (공격/막기/패링/잡기) — AI 측 행동은 확률 기반 플레이스홀더
9. ✅ 존버 방지 시스템
10. ✅ FOV 시야 시스템 (SpotLight+실시간 그림자 근사 구현, 셰이더 기반 아님)
11. ✅ 원거리 무기 (칼 던지기 → 권총 파밍, 총알 별도 파밍은 스코프 축소)
12. ✅ 군집 페널티 시스템 (로직 구현 완료, 팀원 1명이라 실전 미검증)
13. ✅ 승리 조건 (전멸=승리 / Player 사망=패배)

Phase 1 코드 구현 자체는 13/13 완료. 단, 미니맵이 없어 존버 노출·군집 레이더·사운드 핑이 화면에 시각적으로 드러나지 않고, FOV/군집 페널티/A·B 사이트 배치는 실제 플레이로 확인 전이라 튜닝 여지가 큼 — "완료"는 스크립트·씬 배선 기준이며 플레이 테스트로 검증된 것과는 별개.

### Phase 2 — 코어 게임플레이
- 요원 스킬 구현 (방어형/공격형)
- 은신 시스템 고도화
- UI/HUD 완성 (미니맵, 탄약, 팀원 상태)
- 밸런싱

### Phase 3 — 멀티플레이
- Photon PUN2로 시작 (빠른 프로토타입)
- 추후 Mirror 전환 검토 (스팀 출시 전)
- 3vs3 / 5vs5 매칭

### Phase 4 — 공포 PvE 모드
- 사운드 시스템 재활용 (몬스터가 소리로 추적)
- FOV 극도로 제한
- 심박수 UI, 조명 연출
- 숨죽이기 메커닉

---

## 밸런싱 예정 항목
- 요원별 이동속도 수치
- 암살 가능 거리/각도
- 발소리 반경 수치
- 존버 패널티 타이머
- CQC 패링 타이밍 윈도우 (현재 0.3초)
- CQC 공격 피해량/막기 감소율/경직 지속시간 (현재 2dmg / 50% 감소 / 1.5초)
- AI CQC 행동 패턴 (현재 60% 공격 · 40% 막기 확률 기반 플레이스홀더 — 실제 전투 AI로 교체 필요)
- 군집 감지 반경 (현재 5m) / 군집 인원 임계값 (현재 3명)
- CQC 피해 증가율 (현재 1.5배)
- 레이더 노출 주기/지속 시간 (미니맵 없어 미구현)
- FOV SpotLight 밝기(5)/각도(100°)/사거리(14m), 태양광 감광 정도(0.25) — 실제 플레이로 본 적 없는 1차 추정치
- 칼 던지기 속도(18m/s)/회수 거리(1.5m)/자동 소멸 시간(6초)
- 권총 사거리(30m)
- A/B 사이트 위치 (Player 스폰 기준 상대 오프셋으로 배치 — 실제 맵 형태 확인 후 재배치 필요할 수 있음)
